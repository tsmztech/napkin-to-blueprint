## Summary

<!-- What changed and why. Link the issue if there is one. -->

## Test plan

- [ ] `npm test` passes
- [ ] Installer re-run against a scratch project for the affected runtime(s)
- [ ] Baseline refreshed with `node test/install.test.js --update-baseline` (only if `commands/` or `n2b/` changed on purpose)
- [ ] `README.md` updated (only if a command, stage, agent, runtime, or export target changed)
