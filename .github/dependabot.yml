version: 2
updates:
  # The installer has zero npm dependencies, so only GitHub Actions need updates.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(ci):"
